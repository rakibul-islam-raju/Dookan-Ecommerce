export const envVars = {
	BASE_API_URL: import.meta.env.VITE_API_BASE_URL,
};

export const localStorageKeys = {
	ACCESS_TOKEN: "dookan_access",
	REFRESH_TOKEN: "dookan_refresh",
};

export const pagination = {
	limit: 10,
};
