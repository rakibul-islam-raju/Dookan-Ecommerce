1. After placing an order both customer and admin should see the product variant in the order details page. Currently they cant. Also the product item image URL should return from API with full url. currently getting this `"image": "/media/products/a-premium-xxl-3xl-plus-size-womens-short-tunic-top.jpg"`

2. While checking out as a customer - there is a button for save the address for future use. If i click the button multiple times the the address is saving multiple times. instead it could be a checkbox if user select then it will save the address when user confirm the order.

3. After placing an order the stock is getting descresed. but if the order get canceled the again the stock should increase by the quantity of the order for specific variants.

4. In the email template for an order showing this - `Total: 1590.0000 BDT`. show only two digits after the ".". Also not showing the shipping charge `Shipping: 0.00 BDT`. This needs to get fixed
